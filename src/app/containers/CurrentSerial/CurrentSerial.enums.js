const enums = {
    initialState : {
        currentSeason: 1,
        currentVideo: 1
    },
    UPDATE: 'UPDATE@CURRENT_SERIAL',
    REQUEST_VIDEOS: 'REQUEST_VIDEOS@CURRENT_SERIAL',
    LOAD_VIDEOS: 'LOAD_VIDEOS@CURRENT_SERIAL'
};

export default enums;